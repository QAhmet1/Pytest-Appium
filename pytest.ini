[pytest]
addopts = -q
testpaths = tests
markers =
    smoke: quick sanity tests
    regression: full regression suite

filterwarnings =
    # 3rd‑party httplib2 deprecation warnings (google-api-python-client dependency)
    ignore::DeprecationWarning:httplib2.auth
    # Hide google.generativeai FutureWarning (we already know it is deprecated)
    ignore:All support for the `google.generativeai` package has ended:FutureWarning



